<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:api-gateway="http://www.mulesoft.org/schema/mule/api-gateway"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/api-gateway http://www.mulesoft.org/schema/mule/api-gateway/current/mule-api-gateway.xsd">

  <configuration-properties file="application.yaml" />

  <http:listener-config name="proxy-http-listener-config">
    <http:listener-connection host="${http.host}" port="${http.port}" />
  </http:listener-config>

  <http:request-config name="upstream-http-request-config">
    <http:request-connection protocol="${upstream.protocol}"
                             host="${upstream.host}"
                             port="${upstream.port}" />
  </http:request-config>

  <api-gateway:autodiscovery apiId="${api.manager.id}" flowRef="proxy-main" />

  <flow name="proxy-main">
    <http:listener config-ref="proxy-http-listener-config" path="/api/v1/*">
      <http:response statusCode="#[vars.responseStatus default 200]">
        <http:headers>#[vars.responseHeaders default {}]</http:headers>
      </http:response>
      <http:error-response statusCode="#[vars.responseStatus default 502]">
        <http:body>#[payload]</http:body>
        <http:headers>#[vars.responseHeaders default {}]</http:headers>
      </http:error-response>
    </http:listener>

    <flow-ref name="prepare-proxy-request" />

    <http:request config-ref="upstream-http-request-config"
                  method="#[vars.upstreamMethod]"
                  path="#[vars.upstreamPath]">
      <http:headers>#[vars.upstreamHeaders]</http:headers>
      <http:query-params>#[vars.upstreamQueryParams]</http:query-params>
      <http:response-validator>
        <http:success-status-code-validator values="0..599" />
      </http:response-validator>
    </http:request>

    <set-variable variableName="responseStatus" value="#[attributes.statusCode]" />
    <set-variable variableName="responseHeaders" value="#[attributes.headers]" />

    <error-handler>
      <on-error-propagate type="ANY">
        <set-variable variableName="responseStatus" value="502" />
        <set-variable variableName="responseHeaders" value="#[{'content-type': 'application/json'}]" />
        <set-payload value='#[output application/json --- { error: "BAD_GATEWAY", message: "The upstream service could not be reached" }]' />
      </on-error-propagate>
    </error-handler>
  </flow>

  <sub-flow name="prepare-proxy-request">
    <set-variable variableName="upstreamMethod" value="#[attributes.method]" />
    <set-variable variableName="upstreamPath" value="#[p('upstream.basePath') ++ attributes.requestPath]" />
    <set-variable variableName="upstreamQueryParams" value="#[attributes.queryParams]" />
    <set-variable variableName="upstreamHeaders"
                  value="#[attributes.headers filterObject ((value, key) -&gt; !(['client_id', 'client_secret', 'authorization'] contains lower(key as String)))]" />
  </sub-flow>
</mule>
